cd trscore
git init
git remote add origin https://github.com/bozkurtbey1071/trscore.git
git add .
git commit -m "Yeni TRScore Projesi"
git branch -M main
git push -u origin main
import os
import zipfile
base_dir = "/mnt/data/TRScore_Web"
os.makedirs(base_dir, exist_ok=True)
project_files = {
    "package.json": '''{
  "name": "trscore",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}''',

    "next.config.js": '''/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}
module.exports = nextConfig
''',

    "vercel.json": '''{
  "version": 2,
  "builds": [{ "src": "next.config.js", "use": "@vercel/next" }]
}
''',

    "pages/index.js": '''export default function Home() {
 pages_dir = os.path.join(base_dir, "pages")
os.makedirs(pages_dir, exist_ok=True)
project_files["pages/index.js"] = '''export default function Home() {
  return (
    <div style={{ textAlign: "center", marginTop: "100px" }}>
      <h1>TRScore'e Hoş Geldiniz ⚽</h1>
      <p>Yapay zeka destekli maç analizi platformu</p>
    </div>
  )
}
'''
for filename, content in project_files.items():
    path = os.path.join(base_dir, filename)
    if "/" in filename:
        # Alt klasör varsa ona göre dizin oluştur
        folder = os.path.dirname(path)
        os.makedirs(folder, exist_ok=True)
    with open(path, "w") as f:
        f.write(content)





